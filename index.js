class agent {

    constructor(matricule, nom, prenom, dateNaissance,email) {
      this.matricule = matricule
      this.nom = nom;
      this.prenom = prenom;
      this.dateNaissance = dateNaissance;
      this.email = email;
    }
  }

  class Enquete {

    constructor(numero, nom) {
      this.numero = numero
      this.nom = nom;
    }
  }